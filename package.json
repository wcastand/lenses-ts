{
  "name": "lenses",
  "packageManager": "yarn@3.4.1",
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only --no-notify --exit-child src/index.ts",
    "build": "tsc",
    "test": "jest"
  },
  "devDependencies": {
    "rome": "^11.0.0",
    "typescript": "^5.0.2",
    "vitest": "^0.29.7"
  }
}
